[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["rt-multi-thread", "macros", "fs", "net"] }
tonic = "0.11"
prost = "0.12"
proto = { path = "../proto" }
